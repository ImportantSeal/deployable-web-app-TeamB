import React from 'react';

// näyttää nimen tai logon
const Header = () => {
  return (
    <header className="app-header">
      <h1>Helvetin hieno to-do sovellus</h1>
    </header>
  );
};

export default Header;
